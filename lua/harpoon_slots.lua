-- Single source of truth for harpoon slot keybinds.
-- Used by the tabline component (03_settings) and keymaps (04_keymaps).
-- To add/remove a slot, edit this table — both the tab and the keymap update automatically.
return { "z", "x", "c", "d" }
